# 수집 제외 게시글 ID 목록 (ignoreID.md)

## 1. 크롤링 자체 제외 목록

크롤링·정리 대상에서 **완전히 제외**해야 하는 게시글 ID 목록입니다. (삭제된 글, 광고/홍보성 글, 중복 수집된 글, 사용자가 수동으로 제외 지정한 글 등)
AI 에이전트는 크롤링 전 이 목록을 먼저 확인하여 해당 ID는 `크롤링/N.md`에 수록하지 않습니다.

| ID | 제외 사유 |
|---|---|
| 13400 | (사유 미기재 — 확인 필요) |

## 2. 통계 집계 제외 목록 (`분류 안함` / `❌ 분류 안함` 태그)

`크롤링 파일/1.md`~`29.md`에는 이미 수집되어 있으나(파일에서 삭제하지 않음), 끌올·잡담·소재 요청 등 실질적인 문학 작품이 아니라 `CurrentStats.md` 등 **통계 집계 대상에서만 제외**하는 게시글 목록입니다 (총 44건).
AI 에이전트는 문학 통계를 갱신할 때 아래 ID가 붙은 게시글을 집계에서 제외합니다.

| ID | 제외 사유 |
|---|---|
| 48453 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 49212 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 50897 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 69108 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 91009 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 91658 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 93228 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 95466 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 98741 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 99902 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 103846 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 114725 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 114856 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 114876 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 116176 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 117692 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 123736 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 126802 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 130260 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 130275 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 132320 | `❌ 분류 안함` 태그 — 통계 집계 제외 (소재 요청/아이디어 제시글) |
| 132782 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 133357 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 138132 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 138367 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 141469 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 148381 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 148741 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 148849 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 153517 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 154725 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 154878 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 156941 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 156955 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 161144 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 186536 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 186981 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 191187 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 191191 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 191814 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 195859 | `❌ 분류 안함` 태그 — 통계 집계 제외 (소재 요청/아이디어 제시글) |
| 221642 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 222336 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
| 248026 | `분류 안함` 태그 — 통계 집계 제외 (끌올/잡담성 게시글) |
